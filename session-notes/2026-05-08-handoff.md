# Session Handoff — 2026-05-08

## First thing: read CLAUDE_CAPABILITIES.md in this folder.

## What we did this session

### Workspace built
`/Users/jasonl/Desktop/Claudes-Man&Man-build/` is the operational workspace.
- CLAUDE.md defines the working relationship
- CLAUDE_CAPABILITIES.md is the ground truth for tools
- tools/ has build, check_errors, check_api, stream_logs, sync-skills scripts
- 36 skills audited and live in ~/.claude/skills/
- 7 plugins installed (code-review, feature-dev, commit-commands, pr-review-toolkit, security-guidance, hookify, session-report)
- Two GitHub repos: mm-claude-workspace (this folder) and manifest_and_match_V8 (the app)

### MCP fix — CRITICAL
XcodeBuildMCP and GitHub MCP were only in the Claude Desktop app config, not Claude Code CLI.
Fixed by adding them to `~/.claude/claude_desktop_config.json`.
**They should now be available this session — verify by trying mcp__XcodeBuildMCP__list_sims.**

### Code changes committed to manifest_and_match_V8

**1. Thompson persistence wired (commit 9487265)**
- `OptimizedThompsonEngine.swift`: added `import V7Data`, context property, loads alpha/beta from Core Data on init, saves after every swipe
- `JobDiscoveryCoordinator.swift`: passes context when creating the engine
- Arm IDs: "amber_primary" (amber), "teal_primary" (teal)
- Learning now survives app restart

**2. JSearch health check fixed**
- `JSearchAPIClient.swift`: removed live API ping from healthCheck()
- Problem: startup was firing healthCheck (1 request) + fetchJobs (1 request) = 2 rapid calls = 429 rate limit
- Fix: healthCheck now just verifies key is present, no network call
- User has 200 req/month on free tier, was burning 2 on startup before any jobs loaded

## Current app state
- Builds clean on iOS device (device ID: 00008140-001244112E43801C)
- Only JSearch active as job source (200 req/month free tier)
- App still needs to be tested with the health check fix — user was getting 429 before this fix
- Thompson persistence is wired but untested on device yet

## What to test next
1. Build and run on device from Xcode
2. Verify jobs actually appear in the deck (no more 429)
3. Swipe 10-20 jobs, close app, reopen — run check_thompson_state.sh to verify alpha/beta persisted
4. Use XcodeBuildMCP tools now that they're connected

## App source of truth
`/Users/jasonl/Desktop/ios26_manifest_and_match/manifest_and_match_V8/`
Workspace: `ManifestAndMatchV7.xcworkspace`
